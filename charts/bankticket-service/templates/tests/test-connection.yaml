apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bankticket-service.fullname" . }}-test-connection"
  labels:
    {{- include "bankticket-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bankticket-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
